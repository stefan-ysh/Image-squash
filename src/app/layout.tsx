import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "ImageSquash - Professional Image Compression",
  description: "Compress images with professional quality using advanced WebAssembly technology. Batch processing, real-time preview, and multiple format support.",
  keywords: ["image compression", "squoosh", "webp", "jpeg", "png", "batch processing"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}
